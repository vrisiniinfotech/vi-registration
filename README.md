# Additional registration features for Contao Open Source CMS

The module sends a validation code to the email address provided.
The user data is submitted only when the valid code is submitted.

Newly created user is active and logged in automatically.


## Installation

Install the bundle via Composer:

```
composer require vrisiniinfotech/vi-registration
```


## Configuration

All configuration takes place in the root page of each domain/language.
